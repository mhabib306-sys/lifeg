import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.homebase.iphone',
  appName: 'Homebase',
  webDir: 'www',
  ios: {
    contentInset: 'automatic',
  },
};

export default config;
